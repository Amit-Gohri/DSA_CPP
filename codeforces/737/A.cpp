#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n;
    cin >> n;
    vector<int> arr(n);
    int sumA(0), sumB(0);
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
        sumB += arr[i];
    }

    for (int i = 0; i < n; i++)
    {
        //include arr[i] in suma
        // modify sumB
        //calculate formula
    }
    


    
}


int main(){
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}