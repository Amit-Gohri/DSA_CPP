#include<iostream>
#include<vector>
#include<unordered_map>
// range based for loop
//syntax
// for(range_declaration : rangea_expression){...}
//      range decalaration is a variable of same type of that of the sequence
//      range expression is the identifier of the sequence
//      for maps we use auto type 
int main(){
    using namespace std;
    //std::vector<int> a = {1, 2, 3, 4, 5, 6, 7};
    //std::vector<int>::iterator it;
    std::unordered_map<int,int> umap;
    for(auto it : umap){
        
    }

}