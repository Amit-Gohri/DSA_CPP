#include<bits/stdc++.h>
using namespace std;
vector<int> maxset(vector<int> &A) {
    int size = A.size();
    set<int> sizes;
    for (int i = 0; i < size; i++)
    {
        if (A[i]<0)
            continue;
        {
            
        }
        
        vector<int> result;
        
    }
    
}




