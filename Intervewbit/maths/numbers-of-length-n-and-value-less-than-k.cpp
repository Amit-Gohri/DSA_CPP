#include <bits/stdc++.h>
using namespace std;
void solve(vector<int> &A, int B, int C)
{

    int left, right;
    left = B > 1 ? pow(10, B - 1) : 0;
    right = pow(10, B) - 1;
    

}
int main(){
    cout << (int)pow(10, -1);
}